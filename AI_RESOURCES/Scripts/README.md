# Scripts

Place safe repeatable helper scripts here when they are useful to future agents.
Document required environment variables, AWS profiles, dry-run behavior, and rollback expectations for each script.

The current deployment script remains at repo root as `deploy.sh`.
